{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/uesugitorachiyo/ao-forge/docs/contracts/live-docs-execution-guard-v0.1.schema.json",
  "title": "AO Forge Live Docs Execution Guard v0.1",
  "type": "object",
  "required": [
    "schema_version",
    "status",
    "safe_to_execute",
    "allowed_next_action",
    "first_failing_check",
    "blocking_next_actions",
    "maintenance_suggestions",
    "approved_scope",
    "source_hashes",
    "required_evidence",
    "guards",
    "mutates_repositories",
    "executes_work",
    "calls_providers",
    "release_or_publish_allowed"
  ],
  "properties": {
    "schema_version": {
      "const": "ao.forge.live-docs-execution-guard.v0.1"
    },
    "status": {
      "enum": ["ready", "blocked", "failed"]
    },
    "safe_to_execute": {
      "type": "boolean"
    },
    "allowed_next_action": {
      "type": "string",
      "minLength": 1
    },
    "first_failing_check": {
      "type": "string"
    },
    "blocking_next_actions": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "maintenance_suggestions": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "approved_scope": {
      "type": "object",
      "required": ["repo", "allowed_path_class", "allowed_paths", "mutation_class", "branch"],
      "properties": {
        "repo": {
          "type": "string",
          "pattern": "^[a-z0-9._-]{2,80}$"
        },
        "allowed_path_class": {
          "const": "docs_only"
        },
        "allowed_paths": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/relative_path"
          }
        },
        "mutation_class": {
          "const": "tiny_documentation_change"
        },
        "branch": {
          "type": "string",
          "pattern": "^codex/[a-z0-9._-]{3,80}$"
        }
      },
      "additionalProperties": false
    },
    "source_hashes": {
      "type": "array",
      "minItems": 5,
      "items": {
        "$ref": "#/$defs/evidence_ref"
      }
    },
    "required_evidence": {
      "type": "array",
      "minItems": 5,
      "items": {
        "$ref": "#/$defs/evidence_ref"
      }
    },
    "guards": {
      "type": "object",
      "required": [
        "approval_gate",
        "covenant_ticket",
        "docs_only_allowlist",
        "clean_worktree",
        "rollback_plan",
        "sentinel_no_hold",
        "command_readback"
      ],
      "properties": {
        "approval_gate": {
          "enum": ["passed", "blocked", "failed"]
        },
        "covenant_ticket": {
          "enum": ["passed", "blocked", "failed"]
        },
        "docs_only_allowlist": {
          "enum": ["passed", "blocked", "failed"]
        },
        "clean_worktree": {
          "enum": ["planned", "passed", "blocked", "failed"]
        },
        "rollback_plan": {
          "enum": ["planned", "passed", "blocked", "failed"]
        },
        "sentinel_no_hold": {
          "enum": ["passed", "blocked", "failed"]
        },
        "command_readback": {
          "enum": ["passed", "blocked", "failed"]
        }
      },
      "additionalProperties": false
    },
    "mutates_repositories": {
      "const": false
    },
    "executes_work": {
      "const": false
    },
    "calls_providers": {
      "const": false
    },
    "release_or_publish_allowed": {
      "const": false
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "status": {
            "const": "ready"
          }
        }
      },
      "then": {
        "properties": {
          "safe_to_execute": {
            "const": true
          },
          "first_failing_check": {
            "const": ""
          },
          "blocking_next_actions": {
            "maxItems": 0
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "status": {
            "enum": ["blocked", "failed"]
          }
        }
      },
      "then": {
        "properties": {
          "safe_to_execute": {
            "const": false
          },
          "first_failing_check": {
            "minLength": 1
          },
          "blocking_next_actions": {
            "minItems": 1
          }
        }
      }
    }
  ],
  "additionalProperties": false,
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "relative_path": {
      "type": "string",
      "minLength": 1,
      "not": {
        "anyOf": [
          {
            "pattern": "^/"
          },
          {
            "pattern": "^[A-Za-z]:/"
          },
          {
            "pattern": "(^|/)\\.\\.(?:/|$)"
          },
          {
            "pattern": "^~"
          }
        ]
      }
    },
    "evidence_ref": {
      "type": "object",
      "required": ["name", "path", "schema_version", "status", "sha256"],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "path": {
          "$ref": "#/$defs/relative_path"
        },
        "schema_version": {
          "type": "string",
          "minLength": 1
        },
        "status": {
          "type": "string",
          "minLength": 1
        },
        "sha256": {
          "$ref": "#/$defs/sha256"
        }
      },
      "additionalProperties": false
    }
  }
}
