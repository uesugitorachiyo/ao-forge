name: Release Publish

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Exact signed release tag to publish as a draft"
        required: true
        default: "v0.1.4"
      source_commit:
        description: "Exact source and workflow commit qualified by rehearsal"
        required: true
      release_rehearsal_run_id:
        description: "Successful Release Rehearsal run ID for this tag"
        required: true
      expected_plan_digest:
        description: "Exact SHA-256 of the immutable rehearsal plan"
        required: true
      confirm_publish:
        description: "Type true to create a draft GitHub release"
        required: true
        default: "false"
      exact_confirmation:
        description: "publish-ao-forge:<tag>:<source_commit>:<run_id>:<plan_digest>"
        required: true

permissions:
  actions: read
  contents: write
  id-token: write
  attestations: write

concurrency:
  group: release-publish-${{ github.event.inputs.tag }}
  cancel-in-progress: false

jobs:
  draft-release:
    name: Draft release
    runs-on: ubuntu-latest
    environment: production-release
    env:
      REQUESTED_PUBLISH_TAG: ${{ github.event.inputs.tag }}
      RELEASE_REHEARSAL_RUN_ID: ${{ github.event.inputs.release_rehearsal_run_id }}
      SOURCE_COMMIT: ${{ github.event.inputs.source_commit }}
      EXPECTED_PLAN_DIGEST: ${{ github.event.inputs.expected_plan_digest }}
      CONFIRM_PUBLISH: ${{ github.event.inputs.confirm_publish }}
      EXACT_CONFIRMATION: ${{ github.event.inputs.exact_confirmation }}

    steps:
      - name: Guard explicit publish approval
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${CONFIRM_PUBLISH}" != "true" ]]; then
            echo "confirm_publish must be exactly true" >&2
            exit 1
          fi
          [[ "$SOURCE_COMMIT" =~ ^[0-9a-f]{40}$ ]] || { echo "source_commit must be an exact lowercase SHA" >&2; exit 1; }
          [[ "$RELEASE_REHEARSAL_RUN_ID" =~ ^[1-9][0-9]*$ ]] || { echo "release rehearsal run id is malformed" >&2; exit 1; }
          [[ "$EXPECTED_PLAN_DIGEST" =~ ^[0-9a-f]{64}$ ]] || { echo "expected plan digest is malformed" >&2; exit 1; }
          expected_confirmation="publish-ao-forge:${REQUESTED_PUBLISH_TAG}:${SOURCE_COMMIT}:${RELEASE_REHEARSAL_RUN_ID}:${EXPECTED_PLAN_DIGEST}"
          [[ "$EXACT_CONFIRMATION" == "$expected_confirmation" ]] || { echo "exact confirmation mismatch" >&2; exit 1; }

      - name: Checkout exact publisher source
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.source_commit }}
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Import release signer keys
        shell: bash
        run: |
          set -euo pipefail
          gpg --batch --import docs/release/signers/*.asc

      - name: Resolve publish tag
        shell: bash
        run: |
          set -euo pipefail
          [[ "$GITHUB_SHA" == "$SOURCE_COMMIT" ]] || { echo "workflow source mismatch" >&2; exit 1; }
          [[ "$(git rev-parse HEAD)" == "$SOURCE_COMMIT" ]] || { echo "publisher checkout source mismatch" >&2; exit 1; }
          tag="${REQUESTED_PUBLISH_TAG:-}"
          if [[ -z "$tag" ]]; then
            echo "release publish tag is required" >&2
            exit 1
          fi
          if [[ "$tag" != v* ]]; then
            echo "release publish tag must start with v" >&2
            exit 1
          fi

          echo "AO_FORGE_RELEASE_PREVIEW_TAG=$tag" >> "$GITHUB_ENV"
          echo "AO_FORGE_RELEASE_PUBLISH_TAG=$tag" >> "$GITHUB_ENV"
          echo "AO_FORGE_RELEASE_PUBLISH_OUT=${RUNNER_TEMP}/ao-forge-release-publish" >> "$GITHUB_ENV"
          echo "AO_FORGE_RELEASE_PUBLISH_NOTES=${RUNNER_TEMP}/ao-forge-release-publish/release-notes.md" >> "$GITHUB_ENV"
          echo "tag=$tag" >> "$GITHUB_STEP_SUMMARY"

      - name: Verify signed release tag
        shell: bash
        run: |
          set -euo pipefail
          git fetch --force --tags origin "refs/tags/${AO_FORGE_RELEASE_PUBLISH_TAG}:refs/tags/${AO_FORGE_RELEASE_PUBLISH_TAG}"

          tag_ref="refs/tags/${AO_FORGE_RELEASE_PUBLISH_TAG}"
          if ! git rev-parse -q --verify "${tag_ref}" >/dev/null; then
            echo "release publish tag not found: ${AO_FORGE_RELEASE_PUBLISH_TAG}" >&2
            exit 1
          fi

          tag_type="$(git cat-file -t "${tag_ref}")"
          if [[ "$tag_type" != "tag" ]]; then
            echo "release publish tag must be an annotated tag" >&2
            exit 1
          fi

          git tag -v "${AO_FORGE_RELEASE_PUBLISH_TAG}"
          tag_status="$(mktemp)"
          git verify-tag --raw "${AO_FORGE_RELEASE_PUBLISH_TAG}" >"${tag_status}" 2>&1
          signer_fingerprint="$(awk '/^\[GNUPG:\] VALIDSIG / {print $3; exit}' "${tag_status}")"
          if [[ -z "${signer_fingerprint}" ]]; then
            echo "release tag signer fingerprint not found" >&2
            cat "${tag_status}" >&2
            exit 1
          fi
          SIGNER_FINGERPRINT="${signer_fingerprint}" python3 - <<'PY'
          import json
          import os
          import pathlib
          import sys

          policy = json.loads(pathlib.Path("docs/release/RELEASE-SIGNERS.json").read_text())
          signer = os.environ["SIGNER_FINGERPRINT"].upper()
          active = {
              entry.get("fingerprint", "").replace(" ", "").upper()
              for entry in policy.get("active_signers", [])
              if entry.get("status") == "active"
          }
          if signer not in active:
              print("release tag signer is not in RELEASE-SIGNERS.json", file=sys.stderr)
              sys.exit(1)
          print(f"release_tag_signer={signer}")
          PY
          tag_commit="$(git rev-parse "${tag_ref}^{commit}")"
          if [[ "$tag_commit" != "${GITHUB_SHA}" ]]; then
            echo "release publish tag target mismatch: ${tag_commit} != ${GITHUB_SHA}" >&2
            exit 1
          fi

          echo "signed_release_tag_verified=${AO_FORGE_RELEASE_PUBLISH_TAG}"

      - name: Download release rehearsal evidence
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          if [[ -z "${RELEASE_REHEARSAL_RUN_ID:-}" ]]; then
            echo "release_rehearsal_run_id is required" >&2
            exit 1
          fi
          gh run download "${RELEASE_REHEARSAL_RUN_ID}" \
            --name release-rehearsal-evidence \
            --dir "${RUNNER_TEMP}/release-rehearsal-evidence"

      - name: Validate rehearsal evidence
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          import os
          import pathlib
          import sys

          evidence_root = pathlib.Path(os.environ["RUNNER_TEMP"]) / "release-rehearsal-evidence"
          expected_tag = os.environ["AO_FORGE_RELEASE_PUBLISH_TAG"]
          expected_commit = os.environ["GITHUB_SHA"]
          audits = list(evidence_root.rglob("release-preview-audit.json"))
          inspections = list(evidence_root.rglob("release-preview-inspect.json"))

          errors = []
          if len(audits) != 1:
              errors.append(f"expected exactly one rehearsal audit, found {len(audits)}")
          if len(inspections) != 1:
              errors.append(f"expected exactly one rehearsal inspect JSON, found {len(inspections)}")

          if not errors:
              audit = json.loads(audits[0].read_text())
              inspect = json.loads(inspections[0].read_text())
              if audit.get("status") != "passed" or inspect.get("status") != "passed":
                  errors.append("rehearsal evidence did not pass")
              if audit.get("tag") != expected_tag or inspect.get("tag") != expected_tag:
                  errors.append("rehearsal evidence tag mismatch")
              if audit.get("head_commit") != expected_commit or inspect.get("head_commit") != expected_commit:
                  errors.append("rehearsal evidence commit mismatch")
              if inspect.get("failed_checks") != 0:
                  errors.append("rehearsal failed_checks must be 0")
              if audit.get("mutates_releases") is not False or inspect.get("mutates_releases") is not False:
                  errors.append("rehearsal mutates_releases must be false")
              if audit.get("network_required") is not False or inspect.get("network_required") is not False:
                  errors.append("rehearsal network_required must be false")

          if errors:
              for error in errors:
                  print(f"::error::{error}")
              sys.exit(1)

          print(f"release_rehearsal_run_id={os.environ['RELEASE_REHEARSAL_RUN_ID']}")
          print(f"release_rehearsal_tag={expected_tag}")
          PY

      - name: Import and reverify immutable rehearsed candidates
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          plan_dir="${RUNNER_TEMP}/promotion-plan"
          candidates_dir="${RUNNER_TEMP}/downloaded-candidates"
          reconstructed_dir="${RUNNER_TEMP}/reconstructed-plan"
          mkdir -p "$plan_dir" "$candidates_dir" "$reconstructed_dir" "$AO_FORGE_RELEASE_PUBLISH_OUT" dist
          gh run download "$RELEASE_REHEARSAL_RUN_ID" \
            --name "ao-forge-release-rehearsal-plan-${SOURCE_COMMIT}" \
            --dir "$plan_dir"
          gh run download "$RELEASE_REHEARSAL_RUN_ID" \
            --pattern "ao-forge-release-candidate-*-${SOURCE_COMMIT}" \
            --dir "$candidates_dir"
          plan="$plan_dir/immutable-promotion-plan.json"
          actual_plan_digest=$(sha256sum < "$plan" | awk '{print $1}')
          [[ "$actual_plan_digest" == "$EXPECTED_PLAN_DIGEST" ]] || { echo "immutable plan digest mismatch" >&2; exit 1; }
          VERSION=$(jq -er '.version' "$plan")
          manifest_digest=$(jq -er '.approved_manifest_sha256' "$plan")
          [[ "$(jq -er '.source_sha' "$plan")" == "$SOURCE_COMMIT" ]] || { echo "immutable plan source mismatch" >&2; exit 1; }
          [[ "$(jq -er '.tag' "$plan")" == "$AO_FORGE_RELEASE_PUBLISH_TAG" ]] || { echo "immutable plan tag mismatch" >&2; exit 1; }
          [[ "$(jq -er '.workflow_run_id' "$plan")" == "$RELEASE_REHEARSAL_RUN_ID" ]] || { echo "immutable plan run mismatch" >&2; exit 1; }
          common_args=(
            --version "$VERSION"
            --tag "$AO_FORGE_RELEASE_PUBLISH_TAG"
            --source-sha "$SOURCE_COMMIT"
            --manifest-digest "$manifest_digest"
            --workflow-identity ".github/workflows/release-rehearsal.yml@${SOURCE_COMMIT}"
            --workflow-run-id "$RELEASE_REHEARSAL_RUN_ID"
          )
          python3 scripts/verify-release-rehearsal.py assemble \
            "${common_args[@]}" \
            --candidates-dir "$candidates_dir" \
            --output "$reconstructed_dir/immutable-promotion-plan.json"
          cmp --silent "$plan" "$reconstructed_dir/immutable-promotion-plan.json" || { echo "reconstructed immutable plan mismatch" >&2; exit 1; }
          python3 scripts/verify-release-rehearsal.py verify-plan \
            "${common_args[@]}" \
            --plan "$plan"
          while IFS='|' read -r target public_name; do
            planned_name=$(jq -er --arg target "$target" '.candidates[] | select(.target == $target) | .archive' "$plan")
            expected_digest=$(jq -er --arg target "$target" '.candidates[] | select(.target == $target) | .archive_sha256' "$plan")
            mapfile -t matches < <(find "$candidates_dir" -type f -name "$planned_name")
            [[ "${#matches[@]}" -eq 1 ]] || { echo "candidate archive inventory mismatch for $target" >&2; exit 1; }
            actual_digest=$(sha256sum < "${matches[0]}" | awk '{print $1}')
            [[ "$actual_digest" == "$expected_digest" ]] || { echo "candidate archive digest mismatch" >&2; exit 1; }
            cp "${matches[0]}" "dist/$public_name"
          done <<'EOF'
          linux-x86_64|ao-forge_Linux_x86_64.tar.gz
          macos-aarch64|ao-forge_Darwin_arm64.tar.gz
          windows-x86_64|ao-forge_Windows_x86_64.zip
          EOF
          cp "$plan" "${AO_FORGE_RELEASE_PUBLISH_OUT}/immutable-promotion-plan.json"
          cp "${plan}.sha256" "${AO_FORGE_RELEASE_PUBLISH_OUT}/immutable-promotion-plan.json.sha256"
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "RELEASE_NOTES_PATH=docs/release/V${VERSION}-RELEASE-NOTES.md" >> "$GITHUB_ENV"

      - name: Check release archive license files
        run: scripts/check-license-policy.sh --scan-archives

      - name: Build verification CLI
        run: go build -o "${RUNNER_TEMP}/forge" ./cmd/forge

      - name: Generate and verify checksums
        run: |
          (
            cd dist
            "${RUNNER_TEMP}/forge" artifact checksums \
              --artifact ao-forge_Linux_x86_64.tar.gz \
              --artifact ao-forge_Darwin_arm64.tar.gz \
              --artifact ao-forge_Windows_x86_64.zip \
              --out checksums.txt
            "${RUNNER_TEMP}/forge" artifact verify-checksums --manifest checksums.txt
          )

      - name: Run release preview for draft publish
        run: |
          "${RUNNER_TEMP}/forge" release-preview \
            --workspace . \
            --tag "${AO_FORGE_RELEASE_PREVIEW_TAG}" \
            --artifact dist/ao-forge_Linux_x86_64.tar.gz \
            --artifact dist/ao-forge_Darwin_arm64.tar.gz \
            --artifact dist/ao-forge_Windows_x86_64.zip \
            --artifact dist/checksums.txt \
            --out "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-preview-audit.json"
          "${RUNNER_TEMP}/forge" release-preview inspect --audit "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-preview-audit.json" > "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-preview-inspect.txt"
          "${RUNNER_TEMP}/forge" release-preview inspect --audit "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-preview-audit.json" --json > "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-preview-inspect.json"

      - name: Validate release evidence contracts
        run: |
          "${RUNNER_TEMP}/forge" contract validate --schema docs/contracts/release-preview-audit-v0.1.schema.json --document "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-preview-audit.json"
          "${RUNNER_TEMP}/forge" contract validate --schema docs/contracts/release-preview-inspect-v0.1.schema.json --document "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-preview-inspect.json"
          "${RUNNER_TEMP}/forge" contract validate --schema docs/contracts/release-artifact-inventory-v0.1.schema.json --document examples/release-preview/release-artifact-inventory.v0.1.example.json
          "${RUNNER_TEMP}/forge" contract validate --schema docs/contracts/release-attestation-plan-v0.1.schema.json --document examples/release-preview/release-attestation-plan.v0.1.example.json

      - name: Generate artifact attestations
        uses: actions/attest@v4
        with:
          subject-checksums: dist/checksums.txt

      - name: Verify artifact attestations
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          for artifact in \
            dist/ao-forge_Linux_x86_64.tar.gz \
            dist/ao-forge_Darwin_arm64.tar.gz \
            dist/ao-forge_Windows_x86_64.zip
          do
            gh attestation verify "$artifact" \
              --owner "${GITHUB_REPOSITORY_OWNER}" \
              --format json > "${artifact}.attestation.json"
          done

          python3 - <<'PY'
          import json
          import os
          import pathlib
          import sys

          expected_repo = f"https://github.com/{os.environ['GITHUB_REPOSITORY']}"
          expected_ref = os.environ["GITHUB_REF"]
          expected_sha = os.environ["GITHUB_SHA"]
          expected_workflow = ".github/workflows/release-publish.yml"

          manifest = {}
          for line in pathlib.Path("dist/checksums.txt").read_text().splitlines():
              if not line.strip():
                  continue
              digest, name = line.split(maxsplit=1)
              manifest[name] = digest

          failures = []
          for artifact_name, expected_digest in manifest.items():
              artifact_path = pathlib.Path("dist") / artifact_name
              records = json.loads(pathlib.Path(f"{artifact_path}.attestation.json").read_text())
              matched = False

              for record in records:
                  result = record.get("verificationResult", {})
                  statement = result.get("statement", {})
                  certificate = result.get("signature", {}).get("certificate", {})
                  workflow = (
                      statement.get("predicate", {})
                      .get("buildDefinition", {})
                      .get("externalParameters", {})
                      .get("workflow", {})
                  )
                  subjects = statement.get("subject", [])
                  subject_matches = any(
                      subject.get("name") == artifact_name
                      and subject.get("digest", {}).get("sha256") == expected_digest
                      for subject in subjects
                  )

                  if (
                      subject_matches
                      and workflow.get("repository") == expected_repo
                      and workflow.get("ref") == expected_ref
                      and workflow.get("path") == expected_workflow
                      and certificate.get("sourceRepositoryURI") == expected_repo
                      and certificate.get("sourceRepositoryRef") == expected_ref
                      and certificate.get("sourceRepositoryDigest") == expected_sha
                  ):
                      matched = True
                      break

              if not matched:
                  failures.append(f"{artifact_path}: no verified attestation matched expected release publish identity")

          if failures:
              print("\n".join(failures), file=sys.stderr)
              sys.exit(1)

          print(f"artifact_attestations_verified={len(manifest)}")
          PY

      - name: Generate release evidence bundle
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import hashlib
          import json
          import os
          import pathlib

          publish_out = pathlib.Path(os.environ["AO_FORGE_RELEASE_PUBLISH_OUT"])
          bundle_path = publish_out / "release-evidence-bundle.json"

          def sha256(path):
              digest = hashlib.sha256()
              with path.open("rb") as f:
                  for chunk in iter(lambda: f.read(1024 * 1024), b""):
                      digest.update(chunk)
              return digest.hexdigest()

          artifacts = []
          for line in pathlib.Path("dist/checksums.txt").read_text().splitlines():
              if not line.strip():
                  continue
              digest, name = line.split(maxsplit=1)
              artifacts.append({"name": name, "sha256": digest})

          evidence_paths = [
              ("checksums.txt", pathlib.Path("dist/checksums.txt")),
              ("immutable-promotion-plan.json", publish_out / "immutable-promotion-plan.json"),
              ("immutable-promotion-plan.json.sha256", publish_out / "immutable-promotion-plan.json.sha256"),
              ("release-preview-audit.json", publish_out / "release-preview-audit.json"),
              ("release-preview-inspect.txt", publish_out / "release-preview-inspect.txt"),
              ("release-preview-inspect.json", publish_out / "release-preview-inspect.json"),
              (
                  "release-artifact-inventory.v0.1.example.json",
                  pathlib.Path("examples/release-preview/release-artifact-inventory.v0.1.example.json"),
              ),
              (
                  "release-attestation-plan.v0.1.example.json",
                  pathlib.Path("examples/release-preview/release-attestation-plan.v0.1.example.json"),
              ),
          ]
          evidence_paths.extend((path.name, path) for path in sorted(pathlib.Path("dist").glob("*.attestation.json")))

          bundle = {
              "schema_version": "ao.forge.release-evidence-bundle.v0.1",
              "bundle_id": f"release-evidence-{os.environ['AO_FORGE_RELEASE_PUBLISH_TAG'].lstrip('v')}",
              "repository": os.environ["GITHUB_REPOSITORY"],
              "tag": os.environ["AO_FORGE_RELEASE_PUBLISH_TAG"],
              "head_commit": os.environ["GITHUB_SHA"],
              "workflow": {
                  "name": "Release Publish",
                  "path": ".github/workflows/release-publish.yml",
                  "ref": os.environ["GITHUB_REF"],
                  "run_id": os.environ["GITHUB_RUN_ID"],
                  "run_attempt": os.environ["GITHUB_RUN_ATTEMPT"],
              },
              "release_rehearsal_run_id": os.environ["RELEASE_REHEARSAL_RUN_ID"],
              "signature_policy": {
                  "provider": "github-artifact-attestation",
                  "subject": "release-evidence-bundle.json",
                  "verification": "block-release-on-missing-or-mismatched-attestation",
              },
              "artifacts": artifacts,
              "evidence_files": [
                  {"path": public_path, "sha256": sha256(path)}
                  for public_path, path in evidence_paths
              ],
              "verification_steps": [
                  {
                      "step_id": "verify-bundle-attestation",
                      "description": "Verify release-evidence-bundle.json has a GitHub Artifact Attestation from the Release Publish workflow for the release commit.",
                      "required": True,
                  }
              ],
          }

          bundle_path.write_text(json.dumps(bundle, indent=2, sort_keys=True) + "\n")
          print(f"release_evidence_bundle={bundle_path}")
          PY
          "${RUNNER_TEMP}/forge" contract validate --schema docs/contracts/release-evidence-bundle-v0.1.schema.json --document "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-evidence-bundle.json"

      - name: Attest release evidence bundle
        uses: actions/attest@v4
        with:
          subject-path: ${{ runner.temp }}/ao-forge-release-publish/release-evidence-bundle.json

      - name: Verify release evidence bundle attestation
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          gh attestation verify "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-evidence-bundle.json" \
            --owner "${GITHUB_REPOSITORY_OWNER}" \
            --format json > "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-evidence-bundle.attestation.json"

          python3 - <<'PY'
          import hashlib
          import json
          import os
          import pathlib
          import sys

          bundle_path = pathlib.Path(os.environ["AO_FORGE_RELEASE_PUBLISH_OUT"]) / "release-evidence-bundle.json"
          expected_digest = hashlib.sha256(bundle_path.read_bytes()).hexdigest()
          expected_repo = f"https://github.com/{os.environ['GITHUB_REPOSITORY']}"
          expected_ref = os.environ["GITHUB_REF"]
          expected_sha = os.environ["GITHUB_SHA"]
          expected_workflow = ".github/workflows/release-publish.yml"

          records = json.loads((bundle_path.parent / "release-evidence-bundle.attestation.json").read_text())
          matched = False
          for record in records:
              result = record.get("verificationResult", {})
              statement = result.get("statement", {})
              certificate = result.get("signature", {}).get("certificate", {})
              workflow = (
                  statement.get("predicate", {})
                  .get("buildDefinition", {})
                  .get("externalParameters", {})
                  .get("workflow", {})
              )
              subjects = statement.get("subject", [])
              subject_matches = any(
                  subject.get("digest", {}).get("sha256") == expected_digest
                  for subject in subjects
              )

              if (
                  subject_matches
                  and workflow.get("repository") == expected_repo
                  and workflow.get("ref") == expected_ref
                  and workflow.get("path") == expected_workflow
                  and certificate.get("sourceRepositoryURI") == expected_repo
                  and certificate.get("sourceRepositoryRef") == expected_ref
                  and certificate.get("sourceRepositoryDigest") == expected_sha
              ):
                  matched = True
                  break

          if not matched:
              print("release-evidence-bundle.json: no verified attestation matched expected release publish identity", file=sys.stderr)
              sys.exit(1)

          print("release_evidence_bundle_attestation_verified=true")
          PY

      - name: Validate release notes
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import os
          import pathlib
          import sys

          notes_path = pathlib.Path(os.environ["RELEASE_NOTES_PATH"])
          if not notes_path.is_file():
              print(f"release notes file not found: {notes_path}", file=sys.stderr)
              sys.exit(1)

          text = notes_path.read_text()
          forbidden = ["/Users/", "/tmp/", "gho_", "Bearer ", "Authorization:", "excluded handoff"]
          found = [value for value in forbidden if value in text]
          if found:
              for value in found:
                  print(f"::error::release notes contain forbidden public-unsafe text: {value}")
              sys.exit(1)
          print(f"release_notes={notes_path}")
          PY

      - name: Generate publication release notes
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "${AO_FORGE_RELEASE_PUBLISH_OUT}"
          {
            cat "${RELEASE_NOTES_PATH}"
            printf '\n## Publication Evidence\n\n'
            printf -- '- Commit: `%s`\n' "${GITHUB_SHA}"
            printf -- '- Release Publish run: `https://github.com/%s/actions/runs/%s`\n' "${GITHUB_REPOSITORY}" "${GITHUB_RUN_ID}"
            printf -- '- Release Rehearsal run: `https://github.com/%s/actions/runs/%s`\n' "${GITHUB_REPOSITORY}" "${RELEASE_REHEARSAL_RUN_ID}"
            printf -- '- Release evidence assets: `immutable-promotion-plan.json`, `immutable-promotion-plan.json.sha256`, `release-preview-audit.json`, `release-preview-inspect.json`, `checksums.txt`, `release-evidence-bundle.json`, `release-evidence-bundle.attestation.json`, and `*.attestation.json`.\n'
          } > "${AO_FORGE_RELEASE_PUBLISH_NOTES}"

          python3 - <<'PY'
          import os
          import pathlib
          import sys

          notes_path = pathlib.Path(os.environ["AO_FORGE_RELEASE_PUBLISH_NOTES"])
          text = notes_path.read_text()
          forbidden = ["/Users/", "/tmp/", "gho_", "Bearer ", "Authorization:", "excluded handoff"]
          found = [value for value in forbidden if value in text]
          if found:
              for value in found:
                  print(f"::error::generated release notes contain forbidden public-unsafe text: {value}")
              sys.exit(1)
          print(f"generated_release_notes={notes_path}")
          PY

      - name: Create draft GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          gh release create "${AO_FORGE_RELEASE_PUBLISH_TAG}" \
            dist/ao-forge_Linux_x86_64.tar.gz \
            dist/ao-forge_Darwin_arm64.tar.gz \
            dist/ao-forge_Windows_x86_64.zip \
            dist/checksums.txt \
            dist/*.attestation.json \
            "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-preview-audit.json" \
            "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-preview-inspect.txt" \
            "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-preview-inspect.json" \
            "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-evidence-bundle.json" \
            "${AO_FORGE_RELEASE_PUBLISH_OUT}/release-evidence-bundle.attestation.json" \
            "${AO_FORGE_RELEASE_PUBLISH_OUT}/immutable-promotion-plan.json" \
            "${AO_FORGE_RELEASE_PUBLISH_OUT}/immutable-promotion-plan.json.sha256" \
            examples/release-preview/release-artifact-inventory.v0.1.example.json \
            examples/release-preview/release-attestation-plan.v0.1.example.json \
            --repo "${GITHUB_REPOSITORY}" \
            --target "${GITHUB_SHA}" \
            --title "AO Forge ${AO_FORGE_RELEASE_PUBLISH_TAG}" \
            --notes-file "${AO_FORGE_RELEASE_PUBLISH_NOTES}" \
            --draft

      - name: Upload release publish evidence
        uses: actions/upload-artifact@v7
        with:
          name: release-publish-evidence
          path: |
            dist/ao-forge_Linux_x86_64.tar.gz
            dist/ao-forge_Darwin_arm64.tar.gz
            dist/ao-forge_Windows_x86_64.zip
            dist/checksums.txt
            dist/*.attestation.json
            ${{ runner.temp }}/release-rehearsal-evidence
            ${{ runner.temp }}/ao-forge-release-publish/release-preview-audit.json
            ${{ runner.temp }}/ao-forge-release-publish/release-preview-inspect.txt
            ${{ runner.temp }}/ao-forge-release-publish/release-preview-inspect.json
            ${{ runner.temp }}/ao-forge-release-publish/release-evidence-bundle.json
            ${{ runner.temp }}/ao-forge-release-publish/release-evidence-bundle.attestation.json
            ${{ runner.temp }}/ao-forge-release-publish/immutable-promotion-plan.json
            ${{ runner.temp }}/ao-forge-release-publish/immutable-promotion-plan.json.sha256
            examples/release-preview/release-artifact-inventory.v0.1.example.json
            examples/release-preview/release-attestation-plan.v0.1.example.json
          if-no-files-found: error
          retention-days: 30
